import React from 'react'
import './GridLayot.scss'

function GridLayot({children}) {
    return (
        <div className="gridLayot">
           {children}
        </div>
    )
}

export default GridLayot
